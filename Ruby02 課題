1.puts "初めてのRuby"
2.puts "Ruby"+"始めました"
3. 6 0 9 1
4. [1,2,3,4]
5. num_array = [1,2,3,4]
6. p num_array[0]
   p num_array[1]
   p num_array[2]
   p num_array[3]

7. kawaguchi = { name: 'hiroya kawaguchi',birthday: "1992/11/17",bload: "B" }


課題8.9について訂正しましたので添削お願い致します。

8.
def plus_ruby(a)
  (a)+("ruby")
end
   p plus_ruby("like")
  

def plus_ruby(a)
  (a)+("ruby")
end
   p plus_ruby("yhee")


9.
def plus_one(a)
    (a).next
end
    p plus_one(4)


def plus_one(a)
    (a).next
end
    p plus_one(10)
  
